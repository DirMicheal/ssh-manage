<Page x:Class="SshManage.Views.AgentManagePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:helpers="clr-namespace:SshManage.Helpers"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="900"
      Title="密钥代理管控">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" helpers:StackPanelHelper.Spacing="12">
            <TextBlock Text="密钥代理管控" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
            <TextBlock Text="管理SSH Agent中的密钥，控制代理转发与密钥绑定" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,16,0,16">
            <StackPanel Orientation="Horizontal" helpers:StackPanelHelper.Spacing="12">
                <Button x:Name="BtnAddToAgent" Content="➕ 添加到Agent" Style="{StaticResource PrimaryButtonStyle}" Click="BtnAddToAgent_Click" IsEnabled="False" />
                <Button x:Name="BtnRemoveFromAgent" Content="➖ 从Agent移除" Style="{StaticResource SecondaryButtonStyle}" Click="BtnRemoveFromAgent_Click" IsEnabled="False" />
                <Button x:Name="BtnClearAgent" Content="🗑️ 清空Agent" Style="{StaticResource SecondaryButtonStyle}" Click="BtnClearAgent_Click" />
                <Button x:Name="BtnRefreshAgent" Content="🔄 刷新Agent" Style="{StaticResource SecondaryButtonStyle}" Click="BtnRefreshAgent_Click" />
                <Button x:Name="BtnRefreshKeys" Content="🔑 刷新密钥" Style="{StaticResource SecondaryButtonStyle}" Click="BtnRefreshKeys_Click" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="📂 密钥文件" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12" />
                    <ListView x:Name="LocalKeyListView"
                              Grid.Row="1"
                              BorderThickness="0"
                              Background="Transparent"
                              SelectionChanged="LocalKeyListView_SelectionChanged">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="状态" Width="50">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding StatusIcon}" FontSize="14" HorizontalAlignment="Center" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="120" />
                                <GridViewColumn Header="类型" DisplayMemberBinding="{Binding Type}" Width="70" />
                                <GridViewColumn Header="权限" DisplayMemberBinding="{Binding PermissionStatus}" Width="70" />
                                <GridViewColumn Header="Micheal" Width="60">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding IsMichealKey, Converter={StaticResource BoolToIconConverter}}" HorizontalAlignment="Center" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" helpers:StackPanelHelper.Spacing="8">
                        <TextBlock Text="🔐 Agent中的密钥" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" />
                        <TextBlock x:Name="AgentStatusText" Text="" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
                    </StackPanel>
                    <ListView x:Name="AgentKeyListView"
                              Grid.Row="1"
                              BorderThickness="0"
                              Background="Transparent"
                              SelectionChanged="AgentKeyListView_SelectionChanged">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="150" />
                                <GridViewColumn Header="类型" DisplayMemberBinding="{Binding Type}" Width="100" />
                                <GridViewColumn Header="指纹" DisplayMemberBinding="{Binding Fingerprint}" Width="200" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <TextBlock x:Name="StatusText" Text="就绪" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
        </Border>
    </Grid>
</Page>
