<Page x:Class="SshManage.Views.ConnectionTestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:helpers="clr-namespace:SshManage.Helpers"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="连通性检测">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" helpers:StackPanelHelper.Spacing="12">
            <TextBlock Text="连通性检测" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
            <TextBlock Text="检测SSH站点的网络连通性" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,16,0,16">
            <StackPanel helpers:StackPanelHelper.Spacing="12">
                <StackPanel Orientation="Horizontal" helpers:StackPanelHelper.Spacing="12">
                    <Button x:Name="BtnTestAll" Content="📶 检测全部站点" Style="{StaticResource PrimaryButtonStyle}" Click="BtnTestAll_Click" />
                    <Button x:Name="BtnTestSelected" Content="检测选中站点" Style="{StaticResource SecondaryButtonStyle}" Click="BtnTestSelected_Click" IsEnabled="False" />
                    <Button x:Name="BtnRefresh" Content="🔄 刷新站点" Style="{StaticResource SecondaryButtonStyle}" Click="BtnRefresh_Click" />
                    <Button x:Name="BtnStop" Content="⏹ 停止检测" Style="{StaticResource SecondaryButtonStyle}" Click="BtnStop_Click" IsEnabled="False" />
                </StackPanel>
                <DockPanel>
                    <TextBlock Text="超时时间(秒):" Width="100" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBox x:Name="TimeoutTextBox" Text="10" Width="80" VerticalAlignment="Center" />
                </DockPanel>
                <ProgressBar x:Name="ProgressBar" Height="6" Visibility="Collapsed" />
                <TextBlock x:Name="StatusTextBlock" Text="准备就绪" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="📋 站点列表" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12" />
                <ListView x:Name="SiteListView" 
                          Grid.Row="1" 
                          BorderThickness="0" 
                          Background="Transparent"
                          SelectionChanged="SiteListView_SelectionChanged">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="状态" Width="60">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock x:Name="StatusText" Text="❓" FontSize="16" HorizontalAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="主机名" DisplayMemberBinding="{Binding Host}" Width="150" />
                            <GridViewColumn Header="地址" DisplayMemberBinding="{Binding HostName}" Width="150" />
                            <GridViewColumn Header="端口" DisplayMemberBinding="{Binding Port}" Width="60" />
                            <GridViewColumn Header="用户" DisplayMemberBinding="{Binding User}" Width="100" />
                            <GridViewColumn Header="检测结果" Width="*">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Remark}" TextTrimming="CharacterEllipsis" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
